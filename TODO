-fonction compaction
-verrous (vider les tampons avant de lacher le verrou + attention a l'asynchronisme (msync))
-notification (sockets + verifier derniere date modif)

-modif texte fichier

-Faire les close(fd) adequates
-creer fichier dazibao
-tests (TLV inconnus) ?